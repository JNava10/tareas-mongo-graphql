const baseRoute = '/api';
const userDefaultPassword = 'daw';

// ERROR CODES //

const uniqueDuplicatedCode = 11000;

// OTHER //

const roleNames = {
    developer: 'developer',
    admin: 'admin'
}

module.exports = {
    baseRoute,
    userDefaultPassword,
    uniqueDuplicatedCode,
    roleNames
}